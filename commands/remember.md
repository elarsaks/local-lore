---
description: Search offline Claude Code session memory
argument-hint: <question about past work>
---

Search LocalLore for evidence that answers this question:

$ARGUMENTS

Use the LocalLore MCP search tool before answering. Begin with the user's wording, and prefer evidence from the current project unless the question names another project. Use project, date, and file filters when the question supplies those clues. Fetch surrounding context only when the first results are insufficient.

Synthesize the past work instead of dumping raw rows. Mention useful dates, projects, and files; distinguish evidence from inference; and say clearly when no reliable memory was found. Session history can be incomplete, reverted, or superseded.
